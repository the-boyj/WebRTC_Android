<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="model"
            type="com.webrtc.boyj.viewmodel.CallViewModel"/>
        <variable
            name="item"
            type="com.webrtc.boyj.model.dto.User"/>
    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:visibility="@{!model.isCalling}">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_small"
                android:text="@{item.name}"
                android:textColor="@android:color/holo_blue_dark"
                android:textSize="@dimen/text_size_title_main"
                android:gravity="center"/>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{item.tel}"
                android:gravity="center"
                android:textSize="@dimen/text_size_title_small"/>
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:orientation="horizontal"
            android:layout_gravity="bottom"
            android:gravity="center"
            android:layout_marginBottom="@dimen/margin_ultra_big">

            <android.support.design.widget.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/margin_ultra_big"
                android:src="@android:drawable/ic_dialog_dialer"
                app:backgroundTint="@android:color/background_light"
                android:tint="@color/fabForeground"
                app:rippleColor="@android:color/darker_gray" />

            <android.support.design.widget.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/margin_ultra_big"
                android:src="@drawable/ic_call_end"
                android:onClick="@{() -> model.onHangUp()}"
                app:backgroundTint="@android:color/holo_red_dark"
                app:rippleColor="@android:color/white" />

            <android.support.design.widget.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@android:drawable/ic_dialog_info"
                android:tint="@color/fabForeground"
                app:backgroundTint="@android:color/background_light"
                app:rippleColor="@android:color/darker_gray" />
        </LinearLayout>
    </FrameLayout>
</layout>
